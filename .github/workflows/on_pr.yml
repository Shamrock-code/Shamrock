name: on PR

# Controls when the workflow will run
on:
  pull_request:
    types: [opened, synchronize, reopened]

concurrency:
  group: ci-${{github.ref}}-${{github.event.pull_request.number || github.run_number}}
  cancel-in-progress: true

jobs:

  detect_ci_flags:
    name: Detect CI flags
    runs-on: ubuntu-latest
    outputs:
      check_cpp: ${{ steps.flags.outputs.check_cpp }}
      run_sph_tests: ${{ steps.flags.outputs.run_sph_tests }}
      run_ramses_tests: ${{ steps.flags.outputs.run_ramses_tests }}
      run_zeus_tests: ${{ steps.flags.outputs.run_zeus_tests }}
      run_gsph_tests: ${{ steps.flags.outputs.run_gsph_tests }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Diff against PR base
        run: git diff ${{ github.event.pull_request.base.sha }} HEAD > pr.diff
      - name: Detect flags
        id: flags
        run: python3 tools/detect_ci_flags.py --diff pr.diff --github-output "$GITHUB_OUTPUT"

  main_workflow:
    name: CI
    needs: [detect_ci_flags]
    uses: ./.github/workflows/main_workflow.yml
    with:
      run_build_push_docker: true
      light_ci: ${{ contains(github.event.pull_request.labels.*.name, 'light-ci') }}
      # check_cpp: ${{ needs.detect_ci_flags.outputs.check_cpp == 'true' }}
      # run_sph_tests: ${{ needs.detect_ci_flags.outputs.run_sph_tests == 'true' }}
      # run_ramses_tests: ${{ needs.detect_ci_flags.outputs.run_ramses_tests == 'true' }}
      # run_zeus_tests: ${{ needs.detect_ci_flags.outputs.run_zeus_tests == 'true' }}
      # run_gsph_tests: ${{ needs.detect_ci_flags.outputs.run_gsph_tests == 'true' }}

  collect_metrics:
    name: Collect metrics
    needs: [main_workflow]
    if: ${{ always() && !cancelled() }}
    uses: ./.github/workflows/collect_metrics.yml

  upload_metrics_history:
    name: Upload metrics history
    needs: [collect_metrics]
    uses: ./.github/workflows/upload_metrics_history.yml
    with:
      push: false

  light_ci:
    name: Notify Light CI
    if: ${{ contains(github.event.pull_request.labels.*.name, 'light-ci') }}
    runs-on: ubuntu-latest
    steps:
      - name: Echo message
        run: |
          cat >> report_______light_ci.md <<'EOF'


          Light CI is enabled. This will only run the basic tests and not the full tests.
          Merging a PR require the job "on PR / all" to pass which is disabled in this case.
          EOF

      - uses: actions/upload-artifact@v4
        with:
          name: report_light_ci
          path: ./report_______light_ci.md

  # Mergify merge gate (merge_conditions: check-success = all). Skipped when the
  # light-ci label is set — those PRs use all_light for queue entry instead.
  #
  # needs.main_workflow.result / needs.upload_metrics_history.result can be:
  #   - success   : reusable workflow completed without failure
  #   - failure   : at least one job in the called workflow failed
  #   - cancelled : workflow or caller job was cancelled (e.g. concurrency)
  #   - skipped   : caller job was skipped by its if: condition
  #   - abandoned : undocumented by GitHub, but observed in practice when a
  #                 workflow re-run is triggered while the current attempt is
  #                 still finishing (e.g. Mergify CI auto-retry). Treat as
  #                 non-success — do not whitelist only failure/cancelled.
  all:
    needs: [ main_workflow, upload_metrics_history ]
    # run always except for cancel
    if: ${{ always() && !cancelled() && !contains(github.event.pull_request.labels.*.name, 'light-ci') }}
    runs-on: ubuntu-latest
    steps:
    - name: Status summary
      run: |
        if [ "${{ needs.main_workflow.result }}" != "success" ]; then
          echo "CI did not succeed (result: ${{ needs.main_workflow.result }})."
          exit 1
        fi
        if [ "${{ needs.upload_metrics_history.result }}" != "success" ]; then
          echo "Upload metrics history did not succeed (result: ${{ needs.upload_metrics_history.result }})."
          exit 1
        fi

  # Mergify merge-queue entry gate (queue_conditions: check-success = all_light).
  # Runs regardless of the light-ci label.
  # Same gate logic as all; see comment above for needs.*.result states.
  all_light:
    needs: [ main_workflow, upload_metrics_history ]
    # run always except for cancel
    if: ${{ always() && !cancelled() }}
    runs-on: ubuntu-latest
    steps:
    - name: Status summary
      run: |
        if [ "${{ needs.main_workflow.result }}" != "success" ]; then
          echo "CI did not succeed (result: ${{ needs.main_workflow.result }})."
          exit 1
        fi
        if [ "${{ needs.upload_metrics_history.result }}" != "success" ]; then
          echo "Upload metrics history did not succeed (result: ${{ needs.upload_metrics_history.result }})."
          exit 1
        fi
